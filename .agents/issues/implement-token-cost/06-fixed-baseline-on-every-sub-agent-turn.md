# Every sub-agent turn carries about 34k tokens of fixed setup

Category: enhancement
Status: needs-human

## Problem

Every sub-agent turn re-reads about 34k tokens of system prompt, tool definitions and skill listings. In 48 hours of transcripts (2026-09-24 to 2026-09-26), that was 21% of sub-agent cache reads.

Proposed change: turn off plugins and connectors that a repo does not use. Estimated saving: about 2–3% of total spend.

The full measurement is in commit `5d4d45e`, in the Issue this one was split from.

## Comments

> *This was generated by AI during triage.*

**Why this status:** this is host setup that the maintainer does, not a change to these skills.

**What we know:** the setup comes from the plugins, MCP connectors and skills enabled on the host. Claude Code lets you turn them on or off per project in `settings.json`. This repo cannot change which ones a consuming repo enables.

**What is blocked:** nothing in this repo. The maintainer can review the enabled plugins and connectors for each repo where `/implement` runs, for example mvdmio-suite, and turn off the unused ones.
